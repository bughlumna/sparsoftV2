"""
Feature 1 — Type 1 calculation
================================
Replace the stub body with the real formula when ready.
All input parameters are passed in so the function is self-contained
and easy to unit-test independently of the API layer.
"""


def calculateType1(
    alpha: float,
    beta: float,
    sigma_sqr: float,
    mu_0: float,
    mu_1: float,
) -> float:
    """
    Perform the Type 1 calculation for Feature 1.

    Parameters
    ----------
    alpha     : α  — significance level
    beta      : β  — Type II error rate
    sigma_sqr : σ² — variance
    mu_0      : μ₀ — null hypothesis mean
    mu_1      : μ₁ — alternative hypothesis mean

    Returns
    -------
    float — computed result (prototype returns 1.0)
    """
    # TODO: replace with real Type 1 formula
    return 1.0
